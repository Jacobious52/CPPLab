#include "Utils.h"
#include <cstdio>

int main(int argc, char const *argv[])
{
    loop(j, 0, 10)
    {
        printi(j);
    }

    loopi(0, 10)
    {
        printi(i);
    }

    loopij(0, 5, 0, 5)
    {
        printi(i);
        printi(j);
    }

    return 0;
}
